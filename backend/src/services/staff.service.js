import { getModels } from '../models/index.js';
const { User } = getModels();

const create = async (payload) => {
  // TODO: implement create staff
};

const list = async () => {
  // TODO: implement list staff
};

export default { create, list };
