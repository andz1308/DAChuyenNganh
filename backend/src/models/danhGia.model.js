/**
 * Model skeleton for DanhGia
 * TODO: Add attributes based on SQL schema (Danh_Gia_id, ve_id, khach_hang_id, phim_id, noi_dung, diem_rating, ngay_Danh_Gia)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('DanhGia', {
    // TODO: define attributes
  }, { timestamps: false });
};
