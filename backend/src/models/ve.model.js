/**
 * Model skeleton for Ve
 * TODO: Add attributes based on SQL schema (ve_id, ghe_id, Dat_Ve_id, suat_chieu_id, ma_qr_code, trang_thai_ve, gia_ve)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('Ve', {
    // TODO: define attributes
  }, { timestamps: false });
};
