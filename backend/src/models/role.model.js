/**
 * Model skeleton for Role
 * TODO: Add attributes based on SQL schema (role_id, ten_role)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('Role', {
    // TODO: define attributes
  }, { timestamps: false });
};
