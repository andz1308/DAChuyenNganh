/**
 * Model skeleton for LoaiGhe
 * TODO: Add attributes based on SQL schema (loaighe_id, ten_loai, phu_phi)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('LoaiGhe', {
    // TODO: define attributes
  }, { timestamps: false });
};
