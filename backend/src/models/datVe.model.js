/**
 * Model skeleton for DatVe
 * TODO: Add attributes based on SQL schema (Dat_Ve_id, khach_hang_id, nhan_vien_id, ngay_tao, tong_tien, trang_thai_Dat_Ve, ma_giam_gia_id)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('DatVe', {
    // TODO: define attributes
  }, { timestamps: false });
};
