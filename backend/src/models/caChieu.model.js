/**
 * Model skeleton for CaChieu
 * TODO: Add attributes based on SQL schema (ca_chieu_id, gio_bat_dau, gio_ket_thuc)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('CaChieu', {
    // TODO: define attributes
  }, { timestamps: false });
};
