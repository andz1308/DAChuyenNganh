/**
 * Model skeleton for PhongChieu
 * TODO: Add attributes based on SQL schema (phong_chieu_id, rap_id, ten_phong, suc_chua)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('PhongChieu', {
    // TODO: define attributes
  }, { timestamps: false });
};
