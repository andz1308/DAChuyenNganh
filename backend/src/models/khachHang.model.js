/**
 * Model skeleton for KhachHang
 * NOTE: If you consolidate KhachHang into User, this file can be removed/left as backup.
 * TODO: Add attributes based on SQL schema (khach_hang_id, ho_ten, email, so_dien_thoai)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('KhachHang', {
    // TODO: define attributes
  }, { timestamps: false });
};
