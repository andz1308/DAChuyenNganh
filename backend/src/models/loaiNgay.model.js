/**
 * Model skeleton for LoaiNgay
 * TODO: Add attributes based on SQL schema (loai_ngay_id, loai_ngay, phu_phi)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('LoaiNgay', {
    // TODO: define attributes
  }, { timestamps: false });
};
