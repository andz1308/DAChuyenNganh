/**
 * Model skeleton for SuatChieu
 * TODO: Add attributes based on SQL schema (suat_chieu_id, phim_id, phong_chieu_id, ca_chieu_id, ngay_chieu, loai_ngay_id, ngon_ngu, gia_ve)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('SuatChieu', {
    // TODO: define attributes
  }, { timestamps: false });
};
