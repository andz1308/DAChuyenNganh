/**
 * Model skeleton for Rap
 * TODO: Add attributes based on SQL schema (rap_id, ten_rap, dia_chi, email, mo_ta)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('Rap', {
    // TODO: define attributes
  }, { timestamps: false });
};
