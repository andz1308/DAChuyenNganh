/**
 * Model skeleton for Phim_LoaiPhim (junction table)
 * TODO: Add attributes based on SQL schema (phim_id, loaiphim_id)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('Phim_LoaiPhim', {
    // TODO: define attributes
  }, { timestamps: false });
};
