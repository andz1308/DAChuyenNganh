/**
 * Model skeleton for Ghe
 * TODO: Add attributes based on SQL schema (ghe_id, phong_chieu_id, so_ghe, loai_ghe_id, trang_thai)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('Ghe', {
    // TODO: define attributes
  }, { timestamps: false });
};
