/**
 * Model skeleton for KhuyenMai
 * TODO: Add attributes based on SQL schema (ma_giam_gia_id, loai_giam_gia, gia_tri_giam, ngay_bat_dau, ngay_ket_thuc, trang_thai, so_luong_ban_dau, so_luong_con_lai, mo_ta)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('KhuyenMai', {
    // TODO: define attributes
  }, { timestamps: false });
};
