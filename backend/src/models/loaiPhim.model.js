/**
 * Model skeleton for LoaiPhim
 * TODO: Add attributes based on SQL schema (loaiphim_id, ten_loai, mo_ta)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('LoaiPhim', {
    // TODO: define attributes
  }, { timestamps: false });
};
