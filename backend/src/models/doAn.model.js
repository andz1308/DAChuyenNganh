/**
 * Model skeleton for DoAn
 * TODO: Add attributes based on SQL schema (Do_An_id, ten_san_pham, mo_ta, loai, gia)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('DoAn', {
    // TODO: define attributes
  }, { timestamps: false });
};
