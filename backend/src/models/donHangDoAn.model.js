/**
 * Model skeleton for DonHangDoAn
 * TODO: Add attributes based on SQL schema (id, Dat_Ve_id, Do_An_id, so_luong)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('DonHangDoAn', {
    // TODO: define attributes
  }, { timestamps: false });
};
