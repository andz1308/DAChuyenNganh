/**
 * Model skeleton for VaiDien
 * TODO: Add attributes based on SQL schema (id, dien_vien_id, phim_id, ten_vai_dien, hinh_anh, mo_ta)
 */
export default (sequelize, DataTypes) => {
  return sequelize.define('VaiDien', {
    // TODO: define attributes
  }, { timestamps: false });
};
