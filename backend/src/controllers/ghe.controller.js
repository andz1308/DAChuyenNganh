import { getModels } from '../models/index.js';
const { Ghe } = getModels();

const createGhe = async (payload) => {
  // TODO
};

const listGhes = async () => {
  // TODO
};

export default { createGhe, listGhes };
