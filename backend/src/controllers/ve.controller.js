import { getModels } from '../models/index.js';
const { Ve } = getModels();

const createVe = async (payload) => {
  // TODO
};

const listVes = async () => {
  // TODO
};

export default { createVe, listVes };
