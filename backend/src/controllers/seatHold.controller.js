import { getModels } from '../models/index.js';
const { SeatHold } = getModels();

const createSeatHold = async (payload) => {
  // TODO: implement create hold
};

const listSeatHolds = async () => {
  // TODO
};

export default { createSeatHold, listSeatHolds };
