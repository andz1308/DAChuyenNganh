import { getModels } from '../models/index.js';
const { Phim } = getModels();

const createPhim = async (payload) => {
  // TODO
};

const listPhims = async () => {
  // TODO
};

export default { createPhim, listPhims };
